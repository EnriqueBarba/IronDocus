<h1>Welcome admin {{currentUser.fullname}}</h1>

{{#if users}}
    <ul>
    {{#each users as |user|}}
        <li>{{user.fullname}}</li>
    {{/each}}
    </ul>
{{else}}
    <h3>There is not pending users to be validated</h3>
{{/if}}
